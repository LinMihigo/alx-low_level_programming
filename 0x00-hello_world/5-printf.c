#include <stdio.h>
/**
  * main - A C program that prints a string using printf
  * Description: A C program that prints exactly with proper grammar,
  * but the outcome is a piece of art,, followed by a new line.
  * Return: the string "with proper grammar, but the outcome is
  * a piece of art,"
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
